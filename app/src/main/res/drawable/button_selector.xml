<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item>
        <selector>
            <!-- Pressed state -->
            <item android:drawable="@drawable/circle_shape_dark_gray" android:state_pressed="true" />
            <!-- Focused state -->
            <item android:drawable="@drawable/circle_shape_dark_gray" android:state_focused="true" />
            <!-- Hovered state -->
            <item android:drawable="@drawable/circle_shape_dark_gray" android:state_hovered="true" />
            <!-- Default state -->
            <item android:drawable="@drawable/circle_shape_dark_gray"/>
        </selector>
    </item>
</ripple>